package org.celstec.dao.gen;

import java.util.List;
import java.util.ArrayList;
import android.database.Cursor;
import android.database.sqlite.SQLiteDatabase;
import android.database.sqlite.SQLiteStatement;

import de.greenrobot.dao.AbstractDao;
import de.greenrobot.dao.Property;
import de.greenrobot.dao.internal.SqlUtils;
import de.greenrobot.dao.internal.DaoConfig;

import org.celstec.dao.gen.ActionDependencyLocalObject;

// THIS CODE IS GENERATED BY greenDAO, DO NOT EDIT.
/** 
 * DAO for table ACTION_DEPENDENCY_LOCAL_OBJECT.
*/
public class ActionDependencyLocalObjectDao extends AbstractDao<ActionDependencyLocalObject, Void> {

    public static final String TABLENAME = "ACTION_DEPENDENCY_LOCAL_OBJECT";

    /**
     * Properties of entity ActionDependencyLocalObject.<br/>
     * Can be used for QueryBuilder and for referencing column names.
    */
    public static class Properties {
        public final static Property Action = new Property(0, String.class, "action", false, "ACTION");
        public final static Property Scope = new Property(1, Integer.class, "scope", false, "SCOPE");
        public final static Property GeneralItemId = new Property(2, Long.class, "generalItemId", false, "GENERAL_ITEM_ID");
    };

    private DaoSession daoSession;


    public ActionDependencyLocalObjectDao(DaoConfig config) {
        super(config);
    }
    
    public ActionDependencyLocalObjectDao(DaoConfig config, DaoSession daoSession) {
        super(config, daoSession);
        this.daoSession = daoSession;
    }

    /** Creates the underlying database table. */
    public static void createTable(SQLiteDatabase db, boolean ifNotExists) {
        String constraint = ifNotExists? "IF NOT EXISTS ": "";
        db.execSQL("CREATE TABLE " + constraint + "'ACTION_DEPENDENCY_LOCAL_OBJECT' (" + //
                "'ACTION' TEXT," + // 0: action
                "'SCOPE' INTEGER," + // 1: scope
                "'GENERAL_ITEM_ID' INTEGER);"); // 2: generalItemId
    }

    /** Drops the underlying database table. */
    public static void dropTable(SQLiteDatabase db, boolean ifExists) {
        String sql = "DROP TABLE " + (ifExists ? "IF EXISTS " : "") + "'ACTION_DEPENDENCY_LOCAL_OBJECT'";
        db.execSQL(sql);
    }

    /** @inheritdoc */
    @Override
    protected void bindValues(SQLiteStatement stmt, ActionDependencyLocalObject entity) {
        stmt.clearBindings();
 
        String action = entity.getAction();
        if (action != null) {
            stmt.bindString(1, action);
        }
 
        Integer scope = entity.getScope();
        if (scope != null) {
            stmt.bindLong(2, scope);
        }
 
        Long generalItemId = entity.getGeneralItemId();
        if (generalItemId != null) {
            stmt.bindLong(3, generalItemId);
        }
    }

    @Override
    protected void attachEntity(ActionDependencyLocalObject entity) {
        super.attachEntity(entity);
        entity.__setDaoSession(daoSession);
    }

    /** @inheritdoc */
    @Override
    public Void readKey(Cursor cursor, int offset) {
        return null;
    }    

    /** @inheritdoc */
    @Override
    public ActionDependencyLocalObject readEntity(Cursor cursor, int offset) {
        ActionDependencyLocalObject entity = new ActionDependencyLocalObject( //
            cursor.isNull(offset + 0) ? null : cursor.getString(offset + 0), // action
            cursor.isNull(offset + 1) ? null : cursor.getInt(offset + 1), // scope
            cursor.isNull(offset + 2) ? null : cursor.getLong(offset + 2) // generalItemId
        );
        return entity;
    }
     
    /** @inheritdoc */
    @Override
    public void readEntity(Cursor cursor, ActionDependencyLocalObject entity, int offset) {
        entity.setAction(cursor.isNull(offset + 0) ? null : cursor.getString(offset + 0));
        entity.setScope(cursor.isNull(offset + 1) ? null : cursor.getInt(offset + 1));
        entity.setGeneralItemId(cursor.isNull(offset + 2) ? null : cursor.getLong(offset + 2));
     }
    
    /** @inheritdoc */
    @Override
    protected Void updateKeyAfterInsert(ActionDependencyLocalObject entity, long rowId) {
        // Unsupported or missing PK type
        return null;
    }
    
    /** @inheritdoc */
    @Override
    public Void getKey(ActionDependencyLocalObject entity) {
        return null;
    }

    /** @inheritdoc */
    @Override    
    protected boolean isEntityUpdateable() {
        return true;
    }
    
    private String selectDeep;

    protected String getSelectDeep() {
        if (selectDeep == null) {
            StringBuilder builder = new StringBuilder("SELECT ");
            SqlUtils.appendColumns(builder, "T", getAllColumns());
            builder.append(',');
            SqlUtils.appendColumns(builder, "T0", daoSession.getGeneralItemLocalObjectDao().getAllColumns());
            builder.append(" FROM ACTION_DEPENDENCY_LOCAL_OBJECT T");
            builder.append(" LEFT JOIN GENERAL_ITEM_LOCAL_OBJECT T0 ON T.'GENERAL_ITEM_ID'=T0.'_id'");
            builder.append(' ');
            selectDeep = builder.toString();
        }
        return selectDeep;
    }
    
    protected ActionDependencyLocalObject loadCurrentDeep(Cursor cursor, boolean lock) {
        ActionDependencyLocalObject entity = loadCurrent(cursor, 0, lock);
        int offset = getAllColumns().length;

        GeneralItemLocalObject generalItemLocalObject = loadCurrentOther(daoSession.getGeneralItemLocalObjectDao(), cursor, offset);
        entity.setGeneralItemLocalObject(generalItemLocalObject);

        return entity;    
    }

    public ActionDependencyLocalObject loadDeep(Long key) {
        assertSinglePk();
        if (key == null) {
            return null;
        }

        StringBuilder builder = new StringBuilder(getSelectDeep());
        builder.append("WHERE ");
        SqlUtils.appendColumnsEqValue(builder, "T", getPkColumns());
        String sql = builder.toString();
        
        String[] keyArray = new String[] { key.toString() };
        Cursor cursor = db.rawQuery(sql, keyArray);
        
        try {
            boolean available = cursor.moveToFirst();
            if (!available) {
                return null;
            } else if (!cursor.isLast()) {
                throw new IllegalStateException("Expected unique result, but count was " + cursor.getCount());
            }
            return loadCurrentDeep(cursor, true);
        } finally {
            cursor.close();
        }
    }
    
    /** Reads all available rows from the given cursor and returns a list of new ImageTO objects. */
    public List<ActionDependencyLocalObject> loadAllDeepFromCursor(Cursor cursor) {
        int count = cursor.getCount();
        List<ActionDependencyLocalObject> list = new ArrayList<ActionDependencyLocalObject>(count);
        
        if (cursor.moveToFirst()) {
            if (identityScope != null) {
                identityScope.lock();
                identityScope.reserveRoom(count);
            }
            try {
                do {
                    list.add(loadCurrentDeep(cursor, false));
                } while (cursor.moveToNext());
            } finally {
                if (identityScope != null) {
                    identityScope.unlock();
                }
            }
        }
        return list;
    }
    
    protected List<ActionDependencyLocalObject> loadDeepAllAndCloseCursor(Cursor cursor) {
        try {
            return loadAllDeepFromCursor(cursor);
        } finally {
            cursor.close();
        }
    }
    

    /** A raw-style query where you can pass any WHERE clause and arguments. */
    public List<ActionDependencyLocalObject> queryDeep(String where, String... selectionArg) {
        Cursor cursor = db.rawQuery(getSelectDeep() + where, selectionArg);
        return loadDeepAllAndCloseCursor(cursor);
    }
 
}
