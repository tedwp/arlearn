package org.celstec.dao.gen;

import java.util.List;
import java.util.ArrayList;
import android.database.Cursor;
import android.database.sqlite.SQLiteDatabase;
import android.database.sqlite.SQLiteStatement;

import de.greenrobot.dao.AbstractDao;
import de.greenrobot.dao.Property;
import de.greenrobot.dao.internal.SqlUtils;
import de.greenrobot.dao.internal.DaoConfig;
import de.greenrobot.dao.query.Query;
import de.greenrobot.dao.query.QueryBuilder;

import org.celstec.dao.gen.GameContributorLocalObject;

// THIS CODE IS GENERATED BY greenDAO, DO NOT EDIT.
/** 
 * DAO for table GAME_CONTRIBUTOR_LOCAL_OBJECT.
*/
public class GameContributorLocalObjectDao extends AbstractDao<GameContributorLocalObject, Long> {

    public static final String TABLENAME = "GAME_CONTRIBUTOR_LOCAL_OBJECT";

    /**
     * Properties of entity GameContributorLocalObject.<br/>
     * Can be used for QueryBuilder and for referencing column names.
    */
    public static class Properties {
        public final static Property Id = new Property(0, Long.class, "id", true, "_id");
        public final static Property Type = new Property(1, Integer.class, "type", false, "TYPE");
        public final static Property GameId = new Property(2, long.class, "gameId", false, "GAME_ID");
        public final static Property AccountId = new Property(3, long.class, "accountId", false, "ACCOUNT_ID");
    };

    private DaoSession daoSession;

    private Query<GameContributorLocalObject> gameLocalObject_ContributorsQuery;
    private Query<GameContributorLocalObject> accountLocalObject_AccountQuery;

    public GameContributorLocalObjectDao(DaoConfig config) {
        super(config);
    }
    
    public GameContributorLocalObjectDao(DaoConfig config, DaoSession daoSession) {
        super(config, daoSession);
        this.daoSession = daoSession;
    }

    /** Creates the underlying database table. */
    public static void createTable(SQLiteDatabase db, boolean ifNotExists) {
        String constraint = ifNotExists? "IF NOT EXISTS ": "";
        db.execSQL("CREATE TABLE " + constraint + "'GAME_CONTRIBUTOR_LOCAL_OBJECT' (" + //
                "'_id' INTEGER PRIMARY KEY ," + // 0: id
                "'TYPE' INTEGER," + // 1: type
                "'GAME_ID' INTEGER NOT NULL ," + // 2: gameId
                "'ACCOUNT_ID' INTEGER NOT NULL );"); // 3: accountId
    }

    /** Drops the underlying database table. */
    public static void dropTable(SQLiteDatabase db, boolean ifExists) {
        String sql = "DROP TABLE " + (ifExists ? "IF EXISTS " : "") + "'GAME_CONTRIBUTOR_LOCAL_OBJECT'";
        db.execSQL(sql);
    }

    /** @inheritdoc */
    @Override
    protected void bindValues(SQLiteStatement stmt, GameContributorLocalObject entity) {
        stmt.clearBindings();
 
        Long id = entity.getId();
        if (id != null) {
            stmt.bindLong(1, id);
        }
 
        Integer type = entity.getType();
        if (type != null) {
            stmt.bindLong(2, type);
        }
        stmt.bindLong(3, entity.getGameId());
        stmt.bindLong(4, entity.getAccountId());
    }

    @Override
    protected void attachEntity(GameContributorLocalObject entity) {
        super.attachEntity(entity);
        entity.__setDaoSession(daoSession);
    }

    /** @inheritdoc */
    @Override
    public Long readKey(Cursor cursor, int offset) {
        return cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0);
    }    

    /** @inheritdoc */
    @Override
    public GameContributorLocalObject readEntity(Cursor cursor, int offset) {
        GameContributorLocalObject entity = new GameContributorLocalObject( //
            cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0), // id
            cursor.isNull(offset + 1) ? null : cursor.getInt(offset + 1), // type
            cursor.getLong(offset + 2), // gameId
            cursor.getLong(offset + 3) // accountId
        );
        return entity;
    }
     
    /** @inheritdoc */
    @Override
    public void readEntity(Cursor cursor, GameContributorLocalObject entity, int offset) {
        entity.setId(cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0));
        entity.setType(cursor.isNull(offset + 1) ? null : cursor.getInt(offset + 1));
        entity.setGameId(cursor.getLong(offset + 2));
        entity.setAccountId(cursor.getLong(offset + 3));
     }
    
    /** @inheritdoc */
    @Override
    protected Long updateKeyAfterInsert(GameContributorLocalObject entity, long rowId) {
        entity.setId(rowId);
        return rowId;
    }
    
    /** @inheritdoc */
    @Override
    public Long getKey(GameContributorLocalObject entity) {
        if(entity != null) {
            return entity.getId();
        } else {
            return null;
        }
    }

    /** @inheritdoc */
    @Override    
    protected boolean isEntityUpdateable() {
        return true;
    }
    
    /** Internal query to resolve the "contributors" to-many relationship of GameLocalObject. */
    public List<GameContributorLocalObject> _queryGameLocalObject_Contributors(long gameId) {
        synchronized (this) {
            if (gameLocalObject_ContributorsQuery == null) {
                QueryBuilder<GameContributorLocalObject> queryBuilder = queryBuilder();
                queryBuilder.where(Properties.GameId.eq(null));
                gameLocalObject_ContributorsQuery = queryBuilder.build();
            }
        }
        Query<GameContributorLocalObject> query = gameLocalObject_ContributorsQuery.forCurrentThread();
        query.setParameter(0, gameId);
        return query.list();
    }

    /** Internal query to resolve the "account" to-many relationship of AccountLocalObject. */
    public List<GameContributorLocalObject> _queryAccountLocalObject_Account(long accountId) {
        synchronized (this) {
            if (accountLocalObject_AccountQuery == null) {
                QueryBuilder<GameContributorLocalObject> queryBuilder = queryBuilder();
                queryBuilder.where(Properties.AccountId.eq(null));
                accountLocalObject_AccountQuery = queryBuilder.build();
            }
        }
        Query<GameContributorLocalObject> query = accountLocalObject_AccountQuery.forCurrentThread();
        query.setParameter(0, accountId);
        return query.list();
    }

    private String selectDeep;

    protected String getSelectDeep() {
        if (selectDeep == null) {
            StringBuilder builder = new StringBuilder("SELECT ");
            SqlUtils.appendColumns(builder, "T", getAllColumns());
            builder.append(',');
            SqlUtils.appendColumns(builder, "T0", daoSession.getGameLocalObjectDao().getAllColumns());
            builder.append(',');
            SqlUtils.appendColumns(builder, "T1", daoSession.getAccountLocalObjectDao().getAllColumns());
            builder.append(" FROM GAME_CONTRIBUTOR_LOCAL_OBJECT T");
            builder.append(" LEFT JOIN GAME_LOCAL_OBJECT T0 ON T.'GAME_ID'=T0.'_id'");
            builder.append(" LEFT JOIN ACCOUNT_LOCAL_OBJECT T1 ON T.'ACCOUNT_ID'=T1.'_id'");
            builder.append(' ');
            selectDeep = builder.toString();
        }
        return selectDeep;
    }
    
    protected GameContributorLocalObject loadCurrentDeep(Cursor cursor, boolean lock) {
        GameContributorLocalObject entity = loadCurrent(cursor, 0, lock);
        int offset = getAllColumns().length;

        GameLocalObject gameLocalObject = loadCurrentOther(daoSession.getGameLocalObjectDao(), cursor, offset);
         if(gameLocalObject != null) {
            entity.setGameLocalObject(gameLocalObject);
        }
        offset += daoSession.getGameLocalObjectDao().getAllColumns().length;

        AccountLocalObject accountLocalObject = loadCurrentOther(daoSession.getAccountLocalObjectDao(), cursor, offset);
         if(accountLocalObject != null) {
            entity.setAccountLocalObject(accountLocalObject);
        }

        return entity;    
    }

    public GameContributorLocalObject loadDeep(Long key) {
        assertSinglePk();
        if (key == null) {
            return null;
        }

        StringBuilder builder = new StringBuilder(getSelectDeep());
        builder.append("WHERE ");
        SqlUtils.appendColumnsEqValue(builder, "T", getPkColumns());
        String sql = builder.toString();
        
        String[] keyArray = new String[] { key.toString() };
        Cursor cursor = db.rawQuery(sql, keyArray);
        
        try {
            boolean available = cursor.moveToFirst();
            if (!available) {
                return null;
            } else if (!cursor.isLast()) {
                throw new IllegalStateException("Expected unique result, but count was " + cursor.getCount());
            }
            return loadCurrentDeep(cursor, true);
        } finally {
            cursor.close();
        }
    }
    
    /** Reads all available rows from the given cursor and returns a list of new ImageTO objects. */
    public List<GameContributorLocalObject> loadAllDeepFromCursor(Cursor cursor) {
        int count = cursor.getCount();
        List<GameContributorLocalObject> list = new ArrayList<GameContributorLocalObject>(count);
        
        if (cursor.moveToFirst()) {
            if (identityScope != null) {
                identityScope.lock();
                identityScope.reserveRoom(count);
            }
            try {
                do {
                    list.add(loadCurrentDeep(cursor, false));
                } while (cursor.moveToNext());
            } finally {
                if (identityScope != null) {
                    identityScope.unlock();
                }
            }
        }
        return list;
    }
    
    protected List<GameContributorLocalObject> loadDeepAllAndCloseCursor(Cursor cursor) {
        try {
            return loadAllDeepFromCursor(cursor);
        } finally {
            cursor.close();
        }
    }
    

    /** A raw-style query where you can pass any WHERE clause and arguments. */
    public List<GameContributorLocalObject> queryDeep(String where, String... selectionArg) {
        Cursor cursor = db.rawQuery(getSelectDeep() + where, selectionArg);
        return loadDeepAllAndCloseCursor(cursor);
    }
 
}
