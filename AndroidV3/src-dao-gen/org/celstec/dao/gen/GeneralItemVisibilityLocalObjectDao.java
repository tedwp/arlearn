package org.celstec.dao.gen;

import java.util.List;
import java.util.ArrayList;
import android.database.Cursor;
import android.database.sqlite.SQLiteDatabase;
import android.database.sqlite.SQLiteStatement;

import de.greenrobot.dao.AbstractDao;
import de.greenrobot.dao.Property;
import de.greenrobot.dao.internal.SqlUtils;
import de.greenrobot.dao.internal.DaoConfig;
import de.greenrobot.dao.query.Query;
import de.greenrobot.dao.query.QueryBuilder;

import org.celstec.dao.gen.GeneralItemVisibilityLocalObject;

// THIS CODE IS GENERATED BY greenDAO, DO NOT EDIT.
/** 
 * DAO for table GENERAL_ITEM_VISIBILITY_LOCAL_OBJECT.
*/
public class GeneralItemVisibilityLocalObjectDao extends AbstractDao<GeneralItemVisibilityLocalObject, Void> {

    public static final String TABLENAME = "GENERAL_ITEM_VISIBILITY_LOCAL_OBJECT";

    /**
     * Properties of entity GeneralItemVisibilityLocalObject.<br/>
     * Can be used for QueryBuilder and for referencing column names.
    */
    public static class Properties {
        public final static Property Account = new Property(0, String.class, "account", false, "ACCOUNT");
        public final static Property Status = new Property(1, Long.class, "status", false, "STATUS");
        public final static Property TimeStamp = new Property(2, Long.class, "timeStamp", false, "TIME_STAMP");
        public final static Property GeneralItemId = new Property(3, long.class, "generalItemId", false, "GENERAL_ITEM_ID");
        public final static Property RunId = new Property(4, long.class, "runId", false, "RUN_ID");
    };

    private DaoSession daoSession;

    private Query<GeneralItemVisibilityLocalObject> generalItemLocalObject_VisibilitiesQuery;
    private Query<GeneralItemVisibilityLocalObject> runLocalObject_VisibilitiesQuery;

    public GeneralItemVisibilityLocalObjectDao(DaoConfig config) {
        super(config);
    }
    
    public GeneralItemVisibilityLocalObjectDao(DaoConfig config, DaoSession daoSession) {
        super(config, daoSession);
        this.daoSession = daoSession;
    }

    /** Creates the underlying database table. */
    public static void createTable(SQLiteDatabase db, boolean ifNotExists) {
        String constraint = ifNotExists? "IF NOT EXISTS ": "";
        db.execSQL("CREATE TABLE " + constraint + "'GENERAL_ITEM_VISIBILITY_LOCAL_OBJECT' (" + //
                "'ACCOUNT' TEXT," + // 0: account
                "'STATUS' INTEGER," + // 1: status
                "'TIME_STAMP' INTEGER," + // 2: timeStamp
                "'GENERAL_ITEM_ID' INTEGER NOT NULL ," + // 3: generalItemId
                "'RUN_ID' INTEGER NOT NULL );"); // 4: runId
    }

    /** Drops the underlying database table. */
    public static void dropTable(SQLiteDatabase db, boolean ifExists) {
        String sql = "DROP TABLE " + (ifExists ? "IF EXISTS " : "") + "'GENERAL_ITEM_VISIBILITY_LOCAL_OBJECT'";
        db.execSQL(sql);
    }

    /** @inheritdoc */
    @Override
    protected void bindValues(SQLiteStatement stmt, GeneralItemVisibilityLocalObject entity) {
        stmt.clearBindings();
 
        String account = entity.getAccount();
        if (account != null) {
            stmt.bindString(1, account);
        }
 
        Long status = entity.getStatus();
        if (status != null) {
            stmt.bindLong(2, status);
        }
 
        Long timeStamp = entity.getTimeStamp();
        if (timeStamp != null) {
            stmt.bindLong(3, timeStamp);
        }
        stmt.bindLong(4, entity.getGeneralItemId());
        stmt.bindLong(5, entity.getRunId());
    }

    @Override
    protected void attachEntity(GeneralItemVisibilityLocalObject entity) {
        super.attachEntity(entity);
        entity.__setDaoSession(daoSession);
    }

    /** @inheritdoc */
    @Override
    public Void readKey(Cursor cursor, int offset) {
        return null;
    }    

    /** @inheritdoc */
    @Override
    public GeneralItemVisibilityLocalObject readEntity(Cursor cursor, int offset) {
        GeneralItemVisibilityLocalObject entity = new GeneralItemVisibilityLocalObject( //
            cursor.isNull(offset + 0) ? null : cursor.getString(offset + 0), // account
            cursor.isNull(offset + 1) ? null : cursor.getLong(offset + 1), // status
            cursor.isNull(offset + 2) ? null : cursor.getLong(offset + 2), // timeStamp
            cursor.getLong(offset + 3), // generalItemId
            cursor.getLong(offset + 4) // runId
        );
        return entity;
    }
     
    /** @inheritdoc */
    @Override
    public void readEntity(Cursor cursor, GeneralItemVisibilityLocalObject entity, int offset) {
        entity.setAccount(cursor.isNull(offset + 0) ? null : cursor.getString(offset + 0));
        entity.setStatus(cursor.isNull(offset + 1) ? null : cursor.getLong(offset + 1));
        entity.setTimeStamp(cursor.isNull(offset + 2) ? null : cursor.getLong(offset + 2));
        entity.setGeneralItemId(cursor.getLong(offset + 3));
        entity.setRunId(cursor.getLong(offset + 4));
     }
    
    /** @inheritdoc */
    @Override
    protected Void updateKeyAfterInsert(GeneralItemVisibilityLocalObject entity, long rowId) {
        // Unsupported or missing PK type
        return null;
    }
    
    /** @inheritdoc */
    @Override
    public Void getKey(GeneralItemVisibilityLocalObject entity) {
        return null;
    }

    /** @inheritdoc */
    @Override    
    protected boolean isEntityUpdateable() {
        return true;
    }
    
    /** Internal query to resolve the "visibilities" to-many relationship of GeneralItemLocalObject. */
    public List<GeneralItemVisibilityLocalObject> _queryGeneralItemLocalObject_Visibilities(long generalItemId) {
        synchronized (this) {
            if (generalItemLocalObject_VisibilitiesQuery == null) {
                QueryBuilder<GeneralItemVisibilityLocalObject> queryBuilder = queryBuilder();
                queryBuilder.where(Properties.GeneralItemId.eq(null));
                generalItemLocalObject_VisibilitiesQuery = queryBuilder.build();
            }
        }
        Query<GeneralItemVisibilityLocalObject> query = generalItemLocalObject_VisibilitiesQuery.forCurrentThread();
        query.setParameter(0, generalItemId);
        return query.list();
    }

    /** Internal query to resolve the "visibilities" to-many relationship of RunLocalObject. */
    public List<GeneralItemVisibilityLocalObject> _queryRunLocalObject_Visibilities(long runId) {
        synchronized (this) {
            if (runLocalObject_VisibilitiesQuery == null) {
                QueryBuilder<GeneralItemVisibilityLocalObject> queryBuilder = queryBuilder();
                queryBuilder.where(Properties.RunId.eq(null));
                runLocalObject_VisibilitiesQuery = queryBuilder.build();
            }
        }
        Query<GeneralItemVisibilityLocalObject> query = runLocalObject_VisibilitiesQuery.forCurrentThread();
        query.setParameter(0, runId);
        return query.list();
    }

    private String selectDeep;

    protected String getSelectDeep() {
        if (selectDeep == null) {
            StringBuilder builder = new StringBuilder("SELECT ");
            SqlUtils.appendColumns(builder, "T", getAllColumns());
            builder.append(',');
            SqlUtils.appendColumns(builder, "T0", daoSession.getGeneralItemLocalObjectDao().getAllColumns());
            builder.append(',');
            SqlUtils.appendColumns(builder, "T1", daoSession.getRunLocalObjectDao().getAllColumns());
            builder.append(" FROM GENERAL_ITEM_VISIBILITY_LOCAL_OBJECT T");
            builder.append(" LEFT JOIN GENERAL_ITEM_LOCAL_OBJECT T0 ON T.'GENERAL_ITEM_ID'=T0.'_id'");
            builder.append(" LEFT JOIN RUN_LOCAL_OBJECT T1 ON T.'RUN_ID'=T1.'_id'");
            builder.append(' ');
            selectDeep = builder.toString();
        }
        return selectDeep;
    }
    
    protected GeneralItemVisibilityLocalObject loadCurrentDeep(Cursor cursor, boolean lock) {
        GeneralItemVisibilityLocalObject entity = loadCurrent(cursor, 0, lock);
        int offset = getAllColumns().length;

        GeneralItemLocalObject generalItemLocalObject = loadCurrentOther(daoSession.getGeneralItemLocalObjectDao(), cursor, offset);
         if(generalItemLocalObject != null) {
            entity.setGeneralItemLocalObject(generalItemLocalObject);
        }
        offset += daoSession.getGeneralItemLocalObjectDao().getAllColumns().length;

        RunLocalObject runLocalObject = loadCurrentOther(daoSession.getRunLocalObjectDao(), cursor, offset);
         if(runLocalObject != null) {
            entity.setRunLocalObject(runLocalObject);
        }

        return entity;    
    }

    public GeneralItemVisibilityLocalObject loadDeep(Long key) {
        assertSinglePk();
        if (key == null) {
            return null;
        }

        StringBuilder builder = new StringBuilder(getSelectDeep());
        builder.append("WHERE ");
        SqlUtils.appendColumnsEqValue(builder, "T", getPkColumns());
        String sql = builder.toString();
        
        String[] keyArray = new String[] { key.toString() };
        Cursor cursor = db.rawQuery(sql, keyArray);
        
        try {
            boolean available = cursor.moveToFirst();
            if (!available) {
                return null;
            } else if (!cursor.isLast()) {
                throw new IllegalStateException("Expected unique result, but count was " + cursor.getCount());
            }
            return loadCurrentDeep(cursor, true);
        } finally {
            cursor.close();
        }
    }
    
    /** Reads all available rows from the given cursor and returns a list of new ImageTO objects. */
    public List<GeneralItemVisibilityLocalObject> loadAllDeepFromCursor(Cursor cursor) {
        int count = cursor.getCount();
        List<GeneralItemVisibilityLocalObject> list = new ArrayList<GeneralItemVisibilityLocalObject>(count);
        
        if (cursor.moveToFirst()) {
            if (identityScope != null) {
                identityScope.lock();
                identityScope.reserveRoom(count);
            }
            try {
                do {
                    list.add(loadCurrentDeep(cursor, false));
                } while (cursor.moveToNext());
            } finally {
                if (identityScope != null) {
                    identityScope.unlock();
                }
            }
        }
        return list;
    }
    
    protected List<GeneralItemVisibilityLocalObject> loadDeepAllAndCloseCursor(Cursor cursor) {
        try {
            return loadAllDeepFromCursor(cursor);
        } finally {
            cursor.close();
        }
    }
    

    /** A raw-style query where you can pass any WHERE clause and arguments. */
    public List<GeneralItemVisibilityLocalObject> queryDeep(String where, String... selectionArg) {
        Cursor cursor = db.rawQuery(getSelectDeep() + where, selectionArg);
        return loadDeepAllAndCloseCursor(cursor);
    }
 
}
