package org.celstec.dao.gen;

// THIS CODE IS GENERATED BY greenDAO, EDIT ONLY INSIDE THE "KEEP"-SECTIONS

// KEEP INCLUDES - put your custom includes here
import org.celstec.arlearn2.beans.dependencies.ProximityDependency;
import daoBase.DaoConfiguration;
// KEEP INCLUDES END

/**
 * Entity mapped to table PROXIMITY_DEPENDENCY_LOCAL_OBJECT.
 */
public class ProximityDependencyLocalObject extends DependencyLocalObject  {

    private Long radius;
    private Double lat;
    private Double lng;

    // KEEP FIELDS - put your custom fields here
    // KEEP FIELDS END

    public ProximityDependencyLocalObject() {
    }

    public ProximityDependencyLocalObject(Long radius, Double lat, Double lng) {
        this.radius = radius;
        this.lat = lat;
        this.lng = lng;
    }



    public Long getRadius() {
        return radius;
    }

    public void setRadius(Long radius) {
        this.radius = radius;
    }

    public Double getLat() {
        return lat;
    }

    public void setLat(Double lat) {
        this.lat = lat;
    }

    public Double getLng() {
        return lng;
    }

    public void setLng(Double lng) {
        this.lng = lng;
    }

    // KEEP METHODS - put your custom methods here
    public ProximityDependencyLocalObject(ProximityDependency dependsOn) {
        if (dependsOn.getRadius()!=null) setRadius((long)dependsOn.getRadius());
        if (dependsOn.getLat()!=null)setLat(dependsOn.getLat());
        if (dependsOn.getLng()!=null)setLng(dependsOn.getLng());
        DaoConfiguration.getInstance().getDependencyLocalObjectDao().insertOrReplace(this);
    }
    // KEEP METHODS END

}
