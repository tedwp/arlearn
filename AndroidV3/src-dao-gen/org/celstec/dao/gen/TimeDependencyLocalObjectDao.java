package org.celstec.dao.gen;

import android.database.Cursor;
import android.database.sqlite.SQLiteDatabase;
import android.database.sqlite.SQLiteStatement;

import de.greenrobot.dao.AbstractDao;
import de.greenrobot.dao.Property;
import de.greenrobot.dao.internal.DaoConfig;

import org.celstec.dao.gen.TimeDependencyLocalObject;

// THIS CODE IS GENERATED BY greenDAO, DO NOT EDIT.
/** 
 * DAO for table TIME_DEPENDENCY_LOCAL_OBJECT.
*/
public class TimeDependencyLocalObjectDao extends AbstractDao<TimeDependencyLocalObject, Void> {

    public static final String TABLENAME = "TIME_DEPENDENCY_LOCAL_OBJECT";

    /**
     * Properties of entity TimeDependencyLocalObject.<br/>
     * Can be used for QueryBuilder and for referencing column names.
    */
    public static class Properties {
        public final static Property TimeDelta = new Property(0, Long.class, "timeDelta", false, "TIME_DELTA");
    };


    public TimeDependencyLocalObjectDao(DaoConfig config) {
        super(config);
    }
    
    public TimeDependencyLocalObjectDao(DaoConfig config, DaoSession daoSession) {
        super(config, daoSession);
    }

    /** Creates the underlying database table. */
    public static void createTable(SQLiteDatabase db, boolean ifNotExists) {
        String constraint = ifNotExists? "IF NOT EXISTS ": "";
        db.execSQL("CREATE TABLE " + constraint + "'TIME_DEPENDENCY_LOCAL_OBJECT' (" + //
                "'TIME_DELTA' INTEGER);"); // 0: timeDelta
    }

    /** Drops the underlying database table. */
    public static void dropTable(SQLiteDatabase db, boolean ifExists) {
        String sql = "DROP TABLE " + (ifExists ? "IF EXISTS " : "") + "'TIME_DEPENDENCY_LOCAL_OBJECT'";
        db.execSQL(sql);
    }

    /** @inheritdoc */
    @Override
    protected void bindValues(SQLiteStatement stmt, TimeDependencyLocalObject entity) {
        stmt.clearBindings();
 
        Long timeDelta = entity.getTimeDelta();
        if (timeDelta != null) {
            stmt.bindLong(1, timeDelta);
        }
    }

    /** @inheritdoc */
    @Override
    public Void readKey(Cursor cursor, int offset) {
        return null;
    }    

    /** @inheritdoc */
    @Override
    public TimeDependencyLocalObject readEntity(Cursor cursor, int offset) {
        TimeDependencyLocalObject entity = new TimeDependencyLocalObject( //
            cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0) // timeDelta
        );
        return entity;
    }
     
    /** @inheritdoc */
    @Override
    public void readEntity(Cursor cursor, TimeDependencyLocalObject entity, int offset) {
        entity.setTimeDelta(cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0));
     }
    
    /** @inheritdoc */
    @Override
    protected Void updateKeyAfterInsert(TimeDependencyLocalObject entity, long rowId) {
        // Unsupported or missing PK type
        return null;
    }
    
    /** @inheritdoc */
    @Override
    public Void getKey(TimeDependencyLocalObject entity) {
        return null;
    }

    /** @inheritdoc */
    @Override    
    protected boolean isEntityUpdateable() {
        return true;
    }
    
}
